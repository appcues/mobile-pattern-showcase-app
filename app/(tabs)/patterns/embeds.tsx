import { Text, View } from 'react-native';

export default function Embeds() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Detail screen for Embeds!</Text>
    </View>
  );
}
